/**
 * edcheck — semantic validation for Zod schemas, executed server-side.
 *
 * The public surface is introduced change by change (see openspec/changes).
 * Module map and boundaries: openspec/constitution.md §11.
 */
export {};
